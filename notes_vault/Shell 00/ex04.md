### 1. Figure out the right command

> `man ls` // view manual for the `ls` command 

> search for the appropriate options:

- `-p, --indicator-style=slash    aqppend / indicator to directories // appends / to the name of directories

- `-m    fill width with a comma separated list of entries` // separates entries with a comma and a space

- `-t    sort by time, newest first; see --time` // sorts items by modification date

### 2. midLS

> `echo ¨ls -mtp" > midLS` // prints ¨ls -mtp" into a file named "midLS"

> [!check] done


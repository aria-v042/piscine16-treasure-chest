### 1. Create a file with the name \"\\?\$\*\'MaRViN'\*\$\?\\\"

> `touch \"\\\?\$\*\'MaRViN\'\*\$\?\\\"` // creates file with the requested name using "\\"  to escape all of the special characters

### 2. Have the file contain **only** "42" and **nothing else**

`printf`// format and print data --> won't create a newline at the end of the string unless explicitly specified

> `printf "42" > \"\\\?\$\*\'MaRViN\'\*\$\?\\\"` // send exactly "42" to the file, without adding any newline the end of the file

### 3. Modify the file's properties to match the given example

> `chmod 614 \"\\\?\$\*\'MaRViN\'\*\$\?\\\"` // modifies permissions of the file

> can't change the user and group a file belongs to without root permissions





> [!check] done

